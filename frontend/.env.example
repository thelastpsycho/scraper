# Frontend environment values are bundled into JavaScript and visible to every
# browser. NEVER put PMS/DEDGE passwords or API keys into VITE_* variables.
# Store provider/API credentials on the Flask backend (backend/.env), or enter
# them into the authenticated operator UI only when needed.
